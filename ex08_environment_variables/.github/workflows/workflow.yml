name: Environment Workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PROJECT_NAME: "ZeroOps"
      AUTHOR: "DevOpsTeam"
    steps:
      - run: echo "$PROJECT_NAME by $AUTHOR"